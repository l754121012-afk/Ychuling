# FIVESTAR 项目会话约定（Codex 进入本工程自动生效）

## 冷启动

1. 只读 `outputs/SESSION-HANDOFF.md` + 本文件。不要从聊天记录恢复；可复用事实都在交接簿。
2. 脚本/测试导航看 `outputs/CODE-INDEX.md`，按“要改的功能”跳 文件+行。
3. 只执行交接簿“当前任务”里列的事；本次任务之外不碰。

## 上下文卫生（防压缩 / 防重做）

- 一个会话只做**一件**明确的事，做完回填并交用户，再开新会话。
- 大文件（>300 行脚本、设计大文档）用 `rg`/行范围局部读，不整段倾倒。`apply_patch` 改文件，不用 shell 写。
- 地图类任务只读 `outputs/map-construction-blueprint.md`（施工）+ `outputs/map-style-notes.md` + 缩略图 `reference-map-thumb.png`。**大面积参考图 / 大 PNG 永不整读进上下文。**
- 会反复打磨的视觉，第一步先出**便宜可确认**产物（风格要点/低配版）让用户拍板，再上终稿；方向不对就停，不重试旧做法。
- 本会话已确认的结论写进笔记，不为了“确认”重读大文件。

## 硬规则

- 视觉：红=敌攻击预告；金=可送走；白/黄绿=玩家动作反馈；武器中性色。
- 死亡回最近休息点；三小关回第一小关。RMB 满蓄松手→陀螺；X 静止长按0.8s→回血；Q 撤步耗1体力；Shift 冲刺不耗体力。
- 输入语义无歧义：点按/长按/满蓄/松手/静止显式判定且互斥。
- 改参数必须同步视觉表现、命中盒、弧光；改动要能被看见。
- Godot 渲染**必须带窗口**（headless 下 `root.get_texture()` 为 null）；headless 只跑 smoke。

## 收尾

1. 回填 `outputs/SESSION-HANDOFF.md`：改了哪些参数（前后值）、跑了哪些 smoke、预期输出、当前状态与下一步。
2. 可运行则同步更新 `outputs/M1-RUN.md`。
3. 阶段性成果提交；重要基线打 tag（已有 `v1.0-basic-stable` 先例）。
4. 把 smoke 预期输出列入交付消息，再交用户试玩。
