# FIVESTAR 项目会话约定（Codex 每次进入本工程自动生效）

> 本文件是项目级约束，适用于所有指向本工程根目录的 Codex 会话。
> 目标：让会话上下文保持小、不因上下文长度失败、前后一致。

## 进入工程后先做什么

1. 先读 `outputs/SESSION-HANDOFF.md`（交接簿）。
2. 按交接簿的 `## 设计文档索引` 只读指向的文档；**不要通读已定稿的大文档全文**（GDD / 路线 / 地图细化等），需要细节时用关键词定位。
3. 恢复上下文后，只执行交接簿 `## 下一步` 里列出的本次目标。

## 执行时遵守的硬规则

- **只做本次目标**：不顺手重构无关代码、不重写已定稿方案。
- **上下文瘦身**：读大文件用 `rg` / `sed` / 行范围定位，不要整段倾倒；提交用 `apply_patch`，不要用 shell 写文件。
- **视觉与规则**：红=敌攻击预告、金=可送走、白/黄绿=玩家动作反馈、武器中性色；死亡回最近休息点（三小关回第一小关）；RMB 满蓄松手才转陀螺；X 静止长按 0.8s 才回血。
- **输入语义无歧义**：点按/长按/满蓄/松手/静止条件显式判定且互斥。
- **改参数与视觉同步**：参数改了，视觉表现、命中盒、弧光要一起动；改动要能看得到。

## 收尾前必做

1. 回填 `outputs/SESSION-HANDOFF.md`：改了哪些参数（含前后值）、跑了哪些 smoke、预期输出、当前状态与下一步。
2. 可运行则同步更新 `outputs/M1-RUN.md` 的运行命令与预期输出。
3. 阶段性成果 commit；重要基线打 tag（已有 `v1.0-basic-stable` 先例）。
4. 把 smoke 预期输出列入交付消息，再交用户试玩。

## 自然断点：开新会话

- 一个里程碑 / 一个清晰任务 = 一个新会话。**别让一个大会话跨整个开发期。**
- 新会话开头贴 `outputs/SESSION-HANDOFF.md` 里的“新会话启动口令”。
- 只有当需求真正需要用户拍板时才停下来问；能自主推进就推进到底。
