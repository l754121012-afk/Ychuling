# FIVESTAR 项目会话约定（Codex 进入本工程自动生效）

## 上下文事故硬门（2026-09-12）

- 2026-09-12 实测：单个会话日志达到 `237 MB`，发生 `75` 次自动压缩和 `98` 次 `view_image`。根因是反复把原图/大图作为 base64 图片注入上下文；压缩后继续读同一张图，又立即触发下一轮压缩。
- 2026-09-13 再次实测：单个 FIVESTAR 会话日志达到 `198,199,298 B`、`9,246` 条记录，其中 `55` 条单行超过 `1,000,000` 字符；会话中 `view_image` 相关记录 `627` 条（`original` 62、`high` 84），并反复出现压缩/继续摘要。根因仍是图像审阅进入主开发会话，不是任务本身过大。
- FIVESTAR 主开发会话执行“图片零载入”：禁止调用 `view_image`，禁止读取原图、大 PNG/JPG、截图、审图结果或任何 `detail=original/high`。需要在响应里给用户看图时，只输出绝对路径的 Markdown 图片链接；链接不会把像素载入模型上下文。
- 图片生成与图片判断必须拆开：主会话只运行离线脚本、输出尺寸/哈希/像素统计和文件路径，不判断图中内容。确需视觉判断时另开“FIVESTAR 视觉审图”会话，只处理一张指定图，且该会话不得改工程文件。
- 用户把截图作为附件发来时，只把附件路径当成外部真源路径引用；不得再 `view_image` 打开，也不得把图片内容或 base64 重新写进工具输出。
- 首次出现自动压缩、继续摘要或上下文重置迹象时，立即停止读取和修改工作，只回填交接簿，然后开新会话。禁止在压缩上下文里继续试错。
- 禁止读取 `.jsonl` 会话日志或压缩摘要正文；调查上下文问题时只统计体积、次数、记录类型和工具名。

## 冷启动

1. 先读本文件 + `outputs/SESSION-HANDOFF.md` 的“当前交接”一节；不要把交接簿后半段历史默认读入上下文。
2. 脚本/测试导航看 `outputs/CODE-INDEX.md`，大体积产物先看 `outputs/LARGE-ARTIFACT-INDEX.md`，按“要改的功能”跳 文件+行。
3. 只执行交接簿“当前交接”里列的事；本次任务之外不碰。
4. 作者场景类任务只读 `outputs/AUTHORING-WORKFLOW.md` 与指定 Manifest 的命中片段；用户已手调后禁止重建作者场景。
5. 开工先复述任务卡：目标、允许读、允许改、验收、止损。范围不清楚先问，不靠全量重读补上下文。

## 会话预算与止损（硬门槛）

- 一个会话只交付**一个可验收结果**：一个 bug、一个功能节点、一份规格、一次验收或一次排错。包含“参考图分析 → 找模型 → 生成场景 → 接入玩法 → 验证收尾”时必须拆会话，禁止合并。
- 审图任务本身也视为一个独立交付：主会话最多生成一组核对图并列出路径，然后停止等待确认；禁止在同一会话继续改场景、跑游戏或开始下一版图。
- 读前先查大小/行数。超过 `64 KB` 或 `300` 行的文本禁止整读：只允许索引、`rg -n -m`、`Get-Content -TotalCount/-Tail` 或明确行范围；一次输出最多 `120` 行或 `16 KB`。
- `.tscn`、manifest、`.jsonl` 会话日志、构建日志无论大小都禁止整读。需要结论时先生成摘要、统计或命中片段。
- 单次工具输出以 `12,000 tokens` 为上限；预计更大就拆成多个窄查询，不要用一次命令倾倒目录、日志或场景。
- 图片不得把原图/大 PNG 整张注入上下文，也不得在 FIVESTAR 主会话调用 `view_image`。视觉判断只交给独立审图会话；主会话只保留路径、尺寸、哈希和离线统计。
- 雨城灵潮地图只认 `outputs/SESSION-HANDOFF.md` 当前交接、`outputs/spirit-sprawl-topology-v2.md`、`project/authoring/scenes/spirit_sprawl_geometry.tscn`、`project/tools/fs_build_spirit_sprawl_geometry.gd` 和 `project/tests/spirit_sprawl_geometry_smoke.gd`。旧 `map-construction-blueprint`、`map-style-notes`、`reference-map-thumb`、`map-v2-plan`、`map-v2-imagegen-prompt`、`vision-map-reading-template` 与“左下角区域”方案全部作废，不再读取或重新提出。
- 参考图布局对齐先做工程外、简单、像素级概括标注图并交用户确认，再把确认掩膜换算成 cell span；禁止靠在主会话反复读原图硬猜。确认记录和 v4 审查图见 `outputs/spirit-sprawl-topology-v2.md`。
- 首次出现自动压缩、继续摘要或上下文重置迹象时立即停止功能工作和文件写入：只回填交接簿（最后有效状态、已改文件、下一条最小命令），然后新开会话，不在压缩上下文里继续猜。
- 压缩后的新会话只读交接簿“当前交接” + 指定索引/行范围；禁止为“恢复上下文”整读候选场景、全量 Manifest、旧聊天或大日志。
- 失败方向只记录一次。标记为 `FAILED_REFERENCE` 的实验保留文件供排错，但不纳入真源、不继续在其上叠加修改。
- 本会话已确认的结论写进交接簿或任务笔记，不为了“确认”重读大文件。不得用“再全读一遍”解决不确定性。

## 硬规则

- 视觉：红=敌攻击预告；金=可送走；白/黄绿=玩家动作反馈；武器中性色。
- 死亡回最近休息点；三小关回第一小关。RMB 满蓄松手→陀螺；X 静止长按0.8s→回血；Q 撤步耗1体力；Shift 冲刺不耗体力。
- 输入语义无歧义：点按/长按/满蓄/松手/静止显式判定且互斥。
- 改参数必须同步视觉表现、命中盒、弧光；改动要能被看见。
- Godot 渲染**必须带窗口**（headless 下 `root.get_texture()` 为 null）；headless 只跑 smoke。
- 作者场景真源：`project/authoring/scenes/<region>_authoring.tscn`；功能识别只认 `semantic_id`/`kind`/`behavior`/`runtime_support`，不按 Mesh 名或截图猜。
- 作者命名：节点名可含中文 `display_name` 便于查找，但脚本/链接/分组只认 ASCII `semantic_id`；`VISUAL_<model_id>` 只是可视壳，绝不能登记语义或成为行为分支依据。
- 编辑器观察：作者插件 `观察视图` 用 `Ctrl+Alt+1` 从选中物件切入实际游戏视角，`Ctrl+Alt+2` 恢复原编辑视角；共享参数在 `scripts/authoring/FSGameView.gd`，不得只改运行相机造成观察值分叉。
- 构建试玩：默认 `F5` 只加载作者场景、临时 `y=0` 碰撞地面、玩家和相机；完整玩法由 Dock“F5 运行模式”显式切换。模式写入 `fivestar_authoring/playtest_mode`，切换后如仍运行旧值需完全重开项目。

## 收尾

1. 回填 `outputs/SESSION-HANDOFF.md` 的“当前交接”：改了哪些参数（前后值）、跑了哪些 smoke、预期输出、当前状态与下一步；旧过程移出当前交接。
2. 可运行则同步更新 `outputs/M1-RUN.md`。
3. 阶段性成果提交；重要基线打 tag（已有 `v1.0-basic-stable` 先例）。
4. 把 smoke 预期输出列入交付消息，再交用户试玩。
5. 若本会话曾压缩或接近预算，停在新会话接入口，不继续开第二件工作。
