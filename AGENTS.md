# FIVESTAR 项目会话约定（Codex 每次进入本工程自动生效）

> 本文件是项目级强制约束，适用于所有指向本工程根目录的 Codex 会话。
> 目标：让会话上下文保持小、不因上下文长度失败、不重复做同一个决定。

## 进入工程后先做什么

1. 只读两样东西冷启动：`outputs/SESSION-HANDOFF.md` 的「最小上下文块」+ 本文件。
2. 不要读聊天记录、不要从上一个会话的内容里“恢复”。**所有可复用事实都在 `SESSION-HANDOFF.md`。**
3. 按交接簿的「设计文档索引」按需定位，**不要通读已定稿的大文档全文**（GDD / 路线 / 地图细化等）；要细节用 `rg` 关键词定位到具体行。
4. 恢复上下文后，**只执行**交接簿「下一步」里列出的本次任务。本次任务之外的一律不碰。

## 上下文瘦身（强制，防压缩 / 防重做）

- **一个会话只做一件明确的事**。做完、回填交接簿、交用户，再开新会话。绝不让一个会话拖过整个开发期。
- **大文件用 `rg` / `sed` / 行范围定位**，不要整段倾倒。`apply_patch` 改文件，不用 shell 写。
- **大参考图 / 大 PNG 禁止整图读进模型上下文。** 地图类任务只读 `outputs/map-style-notes.md`（风格要点）与 `outputs/reference-map-thumb.png`（512px 缩略图）；原图 `G:/好简历/项目原画/清道夫地图.png` 永不整读。
- **任何视觉资产**，凡需要反复对风格的，先落一个文本“风格要点”文件 + 一张缩略图，把“长什么样”固化下来；后续会话不再从原图重推。
- **先锁意图，再花大成本。** 会反复打磨的视觉 task，第一步先出一个**便宜、可确认**的产物（风格要点 / 低配确认版），让用户拍板方向；方向确认后才做完整渲染。禁止猜着方向直接出终稿。
- **撞到同一堵墙就停。** 同一输出连续被否（例如从“大纲图”被退）时，不要重试旧做法；先改写成文本规格、消歧、锁定范围，再动手。
- **Godot 渲染必须带窗口。** headless 下 `root.get_texture()` 为 null，渲染不出内容；不要用 headless 做渲染类 smoke。
- **读取过的内容不要反复重读。** 本会话内已确认的结论写进笔记即可，不要为“确认”而重读大文件。

## 执行时遵守的硬规则

- **只做本次目标**：不顺手重构无关代码、不重写已定稿方案。
- **视觉与规则**：红=敌攻击预告、金=可送走、白/黄绿=玩家动作反馈、武器中性色；死亡回最近休息点（三小关回第一小关）；RMB 满蓄松手才转陀螺；X 静止长按 0.8s 才回血。
- **输入语义无歧义**：点按/长按/满蓄/松手/静止条件显式判定且互斥。
- **改参数与视觉同步**：参数改了，视觉表现、命中盒、弧光要一起动；改动要能看得到。

## 收尾前必做

1. 回填 `outputs/SESSION-HANDOFF.md`：改了哪些参数（含前后值）、跑了哪些 smoke、预期输出、当前状态与下一步。
2. 可运行则同步更新 `outputs/M1-RUN.md` 的运行命令与预期输出。
3. 阶段性成果 commit；重要基线打 tag（已有 `v1.0-basic-stable` 先例）。
4. 把 smoke 预期输出列入交付消息，再交用户试玩。

## 自然断点：开新会话

- 一个里程碑 / 一个清晰任务 = 一个新会话。**别让一个大会话跨整个开发期。**
- 新会话开头贴 `outputs/SESSION-HANDOFF.md` 里的「新会话启动口令」。
- 只有当需求真正需要用户拍板时才停下来问；能自主推进就推进到底，但推进前确认方向已锁。
