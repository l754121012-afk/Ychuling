# 雨城灵潮基础几何拓扑 v1（历史）

> 历史文件：六区六桥版本已被用户否定的额外识别结果取代。当前真源见 `spirit-sprawl-topology-v2.md`。

> 真源：`G:\好简历\项目原画\Snipaste_2026-09-12_12-52-19.png`。
> 本文只记录基础陆区、水域、岸墙、桥和世界边界，不包含玩法、敌人、灯光、特效或小道具。

## 坐标与边界

- 顶视图约 `521 x 344`，上北下南；世界北向为 `-Z`。
- 映射：`u = px / 521`，`v = py / 344`，`X = -20 + u * 40`，`Z = -13.2 + v * 26.4`。
- 世界边界：`X=-20..20`，`Z=-13.2..13.2`。
- 主地面 `Y=0`；水面/河床为纯视觉层，不提供碰撞承托。
- 施工网格为 `40 x 26`，每格约 `1.0 x 1.015m`；新拓扑是 6 个非对称陆区节点和 6 座桥。
- 旧六岛横排、旧 7 岛 8 桥对称布局、`Z=±8.5` 边界和 `approved_a/review_only` 标记均已作废。

## 陆区

| semantic ID | 名称 | 顶视作用 |
| --- | --- | --- |
| `west_spine` | 西侧折返陆脉 | 左上长而弯折的窄陆脉，上部带向东折返枝，向南收束 |
| `north_knot` | 北中分叉陆区 | 北中部分叉节点，向南逐渐收窄并形成中央长支 |
| `east_mainland` | 东岸连续主陆区 | 右侧最大连续陆区，中北部最宽，南端分叉收束 |
| `northeast_lobe` | 东北离岸枝块 | 东北独立枝块，通过短桥接东岸主陆区 |
| `southwest_lobe` | 西南低台枝块 | 西南枝块，经长桥接西侧陆脉 |
| `southeast_spur` | 东南斜向支块 | 东南短斜枝，经横向桥接中央长支 |

拓扑刻意保留大面积中央和南部水域负空间，不做左右镜像，也不把各岛拉成等宽等距。每行陆区由连续 cell span 生成一个盒体，避免逐格碰撞数量膨胀。

## 水域

- `canal_water` 是覆盖全世界的可视水面与河床，陆区和桥面占据的格位将其切分成不规则水道。
- 水面无碰撞，不提供主地面；玩家不能靠水面承托跨越。
- 水域阻挡依赖岸墙和地面缺口，不用纯贴图承担阻挡。

## 桥

| semantic ID | 轴向 | 连接 |
| --- | --- | --- |
| `br_west_north` | X | 西侧折返陆脉 -> 北中分叉陆区，单格短桥 |
| `br_north_east` | X | 北中分叉陆区 -> 东岸连续主陆区，三格长桥 |
| `br_northeast_east` | Z | 东北离岸枝块 -> 东岸连续主陆区，双格桥面 |
| `br_west_southwest` | Z | 西侧折返陆脉 -> 西南低台枝块，单格竖桥 |
| `br_southwest_center` | X | 西南低台枝块 -> 北中分叉陆区南部，五格横桥 |
| `br_center_southeast` | X | 北中分叉陆区南部 -> 东南斜向支块，四格横桥 |

## 墙体

- `SHORE_*`：由岛 cell 的水岸暴露边自动生成并合并，共 147 段；桥格参与连通判断，桥入口不会生成内部岸墙，桥面侧边保留栏杆视觉。
- `wall_world_w`：`X=-20.3`，长度 `26.4`。
- `wall_world_e`：`X=20.3`，长度 `26.4`。
- `wall_world_n`：`Z=-13.5`，长度 `40.0`。
- `wall_world_s`：`Z=13.5`，长度 `40.0`。

## 施工输出

- 生成器：`project/tools/fs_build_spirit_sprawl_geometry.gd`
- 审核场景：`project/authoring/scenes/spirit_sprawl_geometry.tscn`
- smoke：`project/tests/spirit_sprawl_geometry_smoke.gd`
- 审核场景的基础几何 smoke 通过后，才允许在后续独立会话讨论玩法落点。
